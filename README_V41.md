# V41 渠道结算等级筛选权限修正

V41 基于 V40 累积升级，不改变 V40 的单日日期选择、默认总流水、7 列紧凑表格和真实支付流水口径。

等级查询范围修正为“当前账号可查看的下级代理层级”：

- 超级管理员：一级、二级、三级代理。
- 一级代理：二级、三级代理。
- 二级代理：三级代理。
- 三级代理：不显示渠道结算菜单，后端接口同样无权限。

前端下拉框和后端 API 同步限制，防止手工修改请求参数越权查询。
