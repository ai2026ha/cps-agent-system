# V27 系统资源监控累积修复补丁

这个补丁是累积版，可直接覆盖 V24 / V25 / V26 项目。

修复内容：
- 补齐后端 `/api/system/metrics` 接口。
- CPU / 内存 / 硬盘每 15 秒刷新。
- Render / Docker 优先读取 cgroup 容器资源。
- 单项资源读取失败时，其余指标仍正常返回。
- 前端错误提示会显示具体接口错误，便于定位。
- 保留现有数据总览紧凑布局、彩色图标、已支付平台币流水口径和 RBAC 权限。

覆盖到 GitHub 仓库对应路径即可，等待 Render 自动部署后 Ctrl+F5 强制刷新。
