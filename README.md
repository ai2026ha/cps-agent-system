# V65 更新

玩家中心“领取累充”选择角色/区服后，先显示该角色的当日累充和永久累充，再显示可领取奖励。详见 `README_V65.md`。

## V64 玩家按角色领取累充奖励

- 玩家中心顶部移除当日累充、永久累充展示。
- “领取累充”先选择区服角色，再自动获取该角色当前可领取奖励。
- 累充奖励改为下拉选择，选中后展开奖励内容。
- 累充进度与领取记录按具体角色隔离。
- 详见 `README_V64.md`。

# V63 更新

玩家礼包购买改为单次固定 1 个；礼包选择不显示价格；礼包详情只显示道具与数量；价格合并到购买按钮。详见 `README_V63.md`。

# V62 更新

玩家中心移除“我的商城订单”，新增 CDK 自助兑换入口。CDK 兑换自动绑定当前登录玩家并记录兑换时间；后台商城订单查询保持不变。详见 `README_V62.md`。

## V54 统计口径更新

- 真实支付平台币充值只计真实流水与代理分佣，不增加累计充值。
- 累计充值只由玩家中心网页商城实际消耗平台币购买礼包产生。
- 启动时自动按已支付商城订单重新校准历史累计充值。

## V52：玩家中心与网页礼包商城

- 玩家注册成功后自动跳转玩家中心登录页；登录后可查看平台币余额并使用平台币购买网页商城礼包。
- 商城订单改为玩家购买行为自动生成，后台禁止手工新增商城订单。
- 玩家商城只展示已启用礼包，购买会原子扣减平台币与库存并写入商城订单/平台币账本。
- 商城消费不进入真实充值流水、不增加累计充值、不产生代理分佣。
- 详细说明见 `README_V52.md`。

## V51：平台币订单默认显示全部

- 平台币订单首次打开时日期条件为空，默认显示全部订单。
- 默认按创建时间倒序，今天最新订单在最上方，再依次显示更早日期的订单。
- 开始日期/结束日期仍可用于主动筛选单日或日期区间。
- 点击重置恢复全部订单，不再自动设置为当天。

## V46：平台币订单改为充值支付自动记录

- 平台币订单后台取消“新增”，订单由玩家充值/支付流程自动生成。
- 新增订单号、玩家账号、微信/支付宝、订单状态查询；列表展示商品、金额、发货、创建/支付时间与补发操作。
- 支付成功才计入真实流水；自动发币与支付回调幂等，后台补发不会重复增加流水或分佣。
- 生产环境需配置 `PAYMENT_CALLBACK_SECRET`，详见 `README_V46.md`。

## V34：玩家多区服角色绑定

- 玩家角色数据改为 **一个玩家账号对应多条“区服 + 角色名”绑定记录**，支持同一玩家在多个区服拥有角色。
- 玩家列表不再拆分显示“角色名 / 区服”两列，只保留一个 **角色** 列。
- 角色列默认仅显示主角色名；点击下拉可查看该玩家全部角色及每个角色对应的区服。
- 历史玩家如果已有有效角色名和区服，部署后会自动迁移为一条主角色记录；`未绑定` 数据不会生成虚假角色。
- 新增 `player_characters` 表，旧 `players.role_name/server_name` 字段仅保留兼容，不再作为列表展示数据源。
- 真实游戏角色的自动同步仍需后续接入游戏服务器角色查询/登录回调 API。


## V29：注册地址统一到数据总览

- 所有登录身份都在「数据总览」业务指标下方显示自己的专属玩家注册地址，并支持一键复制。
- 超级管理员拥有总平台直属注册地址：`/register/SUPERADMIN`。通过该地址注册的玩家直属总平台，不绑定普通代理。
- 一级、二级、三级代理继续使用各自 `A1/A2/...` 专属注册地址。
- 玩家列表页面不再重复展示注册链接；注册成功后玩家自动进入对应权限范围的玩家列表。

# CPS 智能代理系统 v1

## V26 超管实时系统资源监控（15 秒刷新）

- 超级管理员的数据总览下方新增 **CPU 使用率 / 内存使用率 / 硬盘使用率**。
- 资源数据每 15 秒自动刷新，并显示百分比、进度条以及内存/磁盘已用与总量。
- Render / Docker 环境优先读取 cgroup 容器资源配额，避免把整台宿主机资源误当成当前 CPS 服务资源。
- 系统资源监控仅超级管理员可见；一级、二级、三级代理无菜单展示且后端接口返回 403。
- 保留 V24 彩色指标图标、V22/V23 紧凑布局，以及“流水只统计已支付平台币订单”的口径。

## V23 更新

- 数据总览的总流水 / 昨日流水 / 今日流水仅统计 **已支付的平台币订单**，商城订单不计入。
- 今日 / 昨日流水优先按支付时间归档，并兼容历史已支付订单的时间字段。
- 继续保留 V22 的紧凑数据总览布局与整体上移效果。


这是一个可运行的 CPS/渠道代理后台第一版，覆盖：

- 渠道管理：下级渠道、自动上下级归属、渠道结算
- 玩家管理：玩家账号、角色、区服、充值与登录数据
- 订单管理：平台币订单、商城订单、发货查询
- 商品管理：礼包列表、商品列表、库存
- 兑换码管理：CDK 批次、生成、兑换与统计
- 累充管理：累充规则、领取记录
- 邮件管理：发送邮件、发送记录（当前为模拟投递，预留游戏服接口）
- 智能运营提醒：发货异常、低库存、待结算、同 IP 多账号风险提示


## V16 权限调整

- 普通代理仅可编辑自己直属下级的 **代理名称** 与 **佣金比例**。
- 修改密码、封禁/解封后台、修改可开通下级数量、修改归属关系仅超级管理员可操作。
- 前端根据当前身份只展示允许编辑的字段；后端再次强制校验，防止绕过前端越权修改。


## V4 左侧菜单结构

后台左侧导航已改为一级总系统 + 二级分功能的树形菜单：

- 一级总系统点击后展开 / 收起，不直接切换业务页面。
- 二级分功能点击后进入对应页面，并保持高亮。
- 一级字体更大、更粗；二级字体更小，层级更清楚。
- 数据总览保留为独立入口。

## 安全说明

系统**不会明文保存管理员、代理、玩家密码**，数据库只保存 Argon2 密码哈希。生产环境必须修改 `JWT_SECRET` 与管理员默认密码，并使用 HTTPS。

## Windows 双击启动（推荐）

1. 安装 **Python 3.11 / 3.12**，安装时勾选 `Add python.exe to PATH`。
2. 解压项目后，直接双击根目录的 **`启动系统.bat`**。
3. 第一次启动会自动创建 `.venv` 并安装依赖；后续直接启动。
4. 服务启动成功后会自动打开浏览器；也可以双击 **`打开后台.bat`**。
5. 后台地址：`http://127.0.0.1:8000`

默认管理员：

- 账号：`admin`
- 密码：`ChangeMe123!`

> 启动后的黑色命令窗口必须保持打开。要停止系统，在该窗口按 `Ctrl+C` 或直接关闭窗口。

> Windows 双击版默认使用本地 SQLite 数据库 `backend/cps.db`，不需要安装 PostgreSQL 或 Docker。


## GitHub + Render 公网部署（推荐）

项目根目录已经提供 `render.yaml`，可以通过 Render Blueprint 一次创建 Web Service + PostgreSQL。

### 1. 上传 GitHub

新建一个 GitHub 仓库，把 `cps-agent-system` 目录中的文件全部上传到仓库根目录。请不要上传 `.env`、本地 `*.db` 或 `.venv`，项目中的 `.gitignore` 已经默认忽略这些文件。

### 2. 在 Render 部署

1. 登录 Render，并连接你的 GitHub。
2. 新建 **Blueprint**，选择刚才的 GitHub 仓库。
3. Render 会读取仓库根目录的 `render.yaml`。
4. 首次创建时，Render 会要求你填写 `ADMIN_PASSWORD`，请使用强密码。
5. 创建完成后等待 Web Service 部署成功。
6. 打开 Render 分配的 `https://xxx.onrender.com` 地址即可访问 CPS 后台。

`render.yaml` 默认使用 **Free Web Service + Free Render Postgres**，适合测试。Render 免费 PostgreSQL 会在创建 30 天后到期，因此真实运营不要长期使用免费数据库。正式运营可以在 Blueprint 创建时改用付费资源，或者使用项目中的 `render-production.yaml` 作为生产配置参考。

### Render 部署结构

- GitHub：保存 CPS 源代码和版本历史。
- Render Web Service：运行 FastAPI 后台和管理界面。
- Render PostgreSQL：保存代理、玩家、订单、CDK、结算、邮件等业务数据。
- 每次 GitHub 默认分支有新提交，Render 自动重新部署 Web Service。

### 重要

- 不要把管理员密码、JWT Secret、数据库密码直接提交到 GitHub。
- `JWT_SECRET` 由 Render 自动生成。
- `ADMIN_PASSWORD` 在首次 Blueprint 部署时由你在 Render 页面输入。
- Render 免费 Web Service 长时间无访问会休眠，下一次打开可能需要等待实例重新启动。
- CPS 进入正式运营后，数据库应切换到付费 Render Postgres，并建立备份策略。

## 快速启动：Docker

```bash
cd cps-agent-system
docker compose up
```

浏览器打开：`http://localhost:8000`

默认管理员：

- 账号：`admin`
- 密码：`ChangeMe123!`

上线前必须修改 `docker-compose.yml` 中的默认密码与 JWT_SECRET。

## 本地 Python 启动

```bash
cd backend
python -m venv .venv
source .venv/bin/activate   # Windows: .venv\\Scripts\\activate
pip install -r requirements.txt
uvicorn app.main:app --reload
```

默认使用 SQLite：`backend/cps.db`。生产推荐 PostgreSQL。

## 核心数据表

- `admin_users` 管理员
- `agents` 代理/渠道
- `players` 玩家
- `platform_coin_orders` 平台币订单
- `mall_orders` 商城订单
- `shipments` 发货记录
- `products` 礼包/商品
- `redemption_batches` CDK 批次
- `redemption_codes` 单个 CDK
- `settlements` 渠道结算
- `recharge_rules` 累充规则
- `claim_records` 领取记录
- `mail_records` 邮件发送记录

## 代理数据库字段

包含：系统自动代理 ID、登录账号、密码哈希、代理名称、系统随机邀请码、自动上级归属、今日流水、昨日流水、总流水、佣金比例、状态。

## 玩家数据库字段

包含：玩家 ID、账号、密码哈希、角色名、区服、所属代理、今日充值、总充值、最后登录时间、登录 IP。

## 下一阶段推荐

1. 接游戏登录/充值回调，订单由平台自动入库，避免后台人工录入。
2. 接游戏服发货 API，实现商品自动发货、失败重试。
3. 接游戏服邮件 API，实现单玩家、区服、全服邮件。
4. 增加 RBAC 权限：超级管理员 / 财务 / 客服 / 渠道管理员。
5. 增加代理独立后台，只能看自己的玩家、订单、下级渠道和佣金。
6. 增加结算审核、打款、账变流水与财务对账。
7. 增加风控：IP、设备、异常充值、订单频率、批量账号识别。
8. 对接支付渠道并实现签名验签、幂等、防重复回调。

## V8 侧栏图标优化

- 一级功能系统使用统一线性 SVG 图标，无外部 CDN 依赖。
- 当前所属一级菜单增加左侧高亮条。
- 菜单 hover / 激活时图标与文字同步高亮。
- 二级分支保持纯文字，继续维持清晰的层级关系。

## V9 三级代理规则

- 超级管理员只能创建一级代理。
- 一级代理只能创建二级代理。
- 二级代理只能创建三级代理。
- 三级代理为末级，不能继续创建代理。
- 新增代理时必须设置“可开通下级代理数量”；三级代理固定为 0。
- 代理 ID、邀请码、上级归属继续由后端自动生成/绑定。
- 已有 Render/PostgreSQL 数据库会在启动时自动补充 `agent_level` 和 `subagent_limit` 字段，无需删库重建；旧账号使用兼容额度，三级代理自动设为 0。

## V9 渠道层级与查询
- 代理等级固定为三级：超管→一级代理→二级代理→三级代理。
- 创建下级时由系统自动绑定上级归属；三级代理不能继续创建下级。
- 每个一级/二级代理可设置直属下级开通数量上限，达到上限后前后端都会禁止继续创建。
- 代理ID与邀请码均由系统自动生成并保证唯一。
- 下级渠道页支持代理账号、代理ID、上级代理，以及流水自定义日期区间查询；选择日期后显示“查询区间流水”。
- 超级管理员的下级渠道查询范围为全部三级代理；代理账号仅能查看自己的直属下级。


## V10 超管与代理等级选择

- `ADMIN_USERNAME` 对应的后台账号在启动时会自动校正为 `superadmin`，兼容旧 Render PostgreSQL 数据库。
- 后台右上角显示当前账号及“超级管理员/代理等级”身份。
- 新增代理的“代理等级”下拉框不再展示不可用的灰色等级，而是要求主动选择当前账号唯一有权开通的等级：超管→一级、一级→二级、二级→三级。
- 三级代理仍是末级，不能继续开通下级。后端继续执行越级校验。


## V11 一级代理上级显示

- 超级管理员创建的一级代理，数据库 `parent_id` 仍保持为空，避免伪造代理父节点。
- 后台“下级渠道”中一级代理的“上级代理”统一显示为 **超管**。
- 二级、三级代理继续显示真实上级代理的代理ID。
- “上级代理查询”输入“超管”或“超级管理员”可直接筛选一级代理。

## V12 渠道管理调整

- 代理ID统一为 `A1`、`A2`、`A3`…，系统自动生成；历史 `AGxxxxxxxx` 会在部署启动时按代理内部序号自动迁移。
- 邀请码与代理ID完全一致。
- 代理列表不再显示“剩余名额”，下级代理额度限制仍由系统后台校验。
- 流水查询支持“昨日 / 今日 / 自定义时间”。
- 新增代理的登录账号、登录密码默认留空，并使用新密码表单属性避免浏览器自动填入旧凭据。


## V15 渠道列表与代理编辑

- 下级渠道页移除流水查询，只保留代理账号、代理ID、上级代理三项查询；查询输入框缩短并左对齐。
- 渠道列表不再展示“下级代理上限”和“已开通下级”两列，额度仍在后台执行严格校验。
- 每个代理最右侧新增“编辑”操作，可修改密码、后台状态、佣金比例、可开通下级数量。
- 超级管理员可在编辑中更改代理归属；二级只能归属一级，三级只能归属二级，一级固定归属超管。
- 普通代理只能编辑自己的直属下级，不能通过接口越权修改其它渠道，也不能自行更改归属。
- 修改下级额度时不能低于当前已开通数量；三级代理额度固定为 0。

## V17：统一后台链接与三级代理权限

V17 起，超级管理员、一级代理、二级代理、三级代理全部使用同一个后台地址和同一个登录入口。系统登录成功后由后端返回账号身份、代理等级和权限清单，前端据此动态显示菜单与操作按钮；后端接口同时执行权限与数据范围校验，不能通过手工请求绕过前端限制。

当前默认权限基线：超级管理员拥有全部权限；一级/二级代理可查看自己及下级渠道树范围内的数据，并按层级开通直属下级；三级代理为末级，不可继续开通代理。普通代理对商品、CDK、累充规则等配置模块默认只读，邮件发送以及系统级新增/修改操作保留给超级管理员。普通代理编辑直属下级时仍只允许修改代理名称和佣金比例。

## V18：代理后台菜单权限收紧

所有账号继续使用同一个后台链接和登录入口，但左侧菜单与后端接口按代理等级严格收紧：

- 超级管理员：显示并可访问全部系统。
- 一级代理：仅显示渠道管理、玩家管理、订单管理；可以按规则开通二级代理。
- 二级代理：仅显示渠道管理、玩家管理、订单管理；可以按规则开通三级代理。
- 三级代理：仅显示玩家管理、订单管理；不显示渠道管理，也不能新增任何代理。
- 数据总览、商品管理、兑换码管理、累充管理、邮件管理等其它系统仅超级管理员可见、可访问。
- 权限不仅由前端隐藏菜单，后端 API 同步校验；三级代理直接请求渠道接口会返回 403。


## V20：代理后台恢复数据总览

- 超级管理员：数据总览 + 全部系统。
- 一级代理：数据总览、渠道管理、玩家管理、订单管理。
- 二级代理：数据总览、渠道管理、玩家管理、订单管理。
- 三级代理：数据总览、玩家管理、订单管理；仍然不显示渠道管理，也不能新增代理。
- 代理的数据总览只统计当前代理权限范围内的数据，不会读取其它渠道的数据。
- 普通代理的数据总览不会显示超管专属的 CDK 全局统计、商品库存告警；三级代理也不会显示渠道结算提醒。

## V21：按角色拆分数据总览

数据总览不再让超管和代理共用同一套指标：

- 超级管理员：注册数据（总注册 / 昨日注册 / 今日注册）、流水数据（总流水 / 昨日流水 / 今日流水）、运营数据（待发货/异常 / 已兑换CDK）。
- 一级、二级、三级代理：注册数据、流水数据、分佣数据（佣金比例 / 昨日分佣 / 今日分佣 / 总计分佣）。
- 所有指标按业务类型分组排列，避免卡片混排。
- 代理数据继续按当前代理及其权限范围内渠道树隔离统计。


## V24 数据总览彩色图标

- 每个总览指标增加内置 SVG 图标，不依赖外部 CDN。
- 注册类使用蓝色，流水类使用绿色，分佣类使用橙色。
- 待发货/异常使用红色，已兑换 CDK 使用紫色。
- 保留 V22/V23 的紧凑布局与“仅统计已支付平台币订单”的流水口径。


## V27 累积监控修复
- 修复只覆盖 V26 前端补丁时 `/api/system/metrics` 后端接口缺失导致“监控暂时不可用”的问题。
- CPU / 内存 / 硬盘仍每 15 秒刷新。
- 单个资源读取失败不会拖垮整个监控接口。


## V28 玩家专属注册地址
- 玩家列表不再提供后台“新增玩家”按钮。
- 每个代理的代理ID/邀请码同时作为专属注册地址标识，例如 `https://你的域名/register/A1`。
- 玩家通过专属注册地址注册账号后，系统自动生成玩家ID（P1/P2...）、自动绑定代理，并出现在该代理权限范围的玩家列表。
- 一级/二级代理仍可查看自己渠道树内玩家；三级代理查看自己范围玩家；超管查看全部玩家。
- 渠道列表提供“复制地址”，代理自己的玩家列表顶部也可直接复制“我的专属注册地址”。

## V30 玩家管理升级

- 数据总览中的专属注册地址改为左侧紧凑布局，注册地址紧挨说明文字与复制按钮。
- 玩家列表顶部新增“账号查询 / 上级代理查询”，查询框使用紧凑宽度。
- 玩家仍只通过专属注册链接注册并自动归属，后台不提供手工新增玩家。
- 超级管理员可在玩家列表最右侧使用“编辑”：修改密码、修改归属、封禁/解封、发放/收回平台币。
- 一级、二级、三级代理只有玩家查看权限，不能看到编辑按钮，后端也拒绝任何玩家修改请求。
- 玩家新增“平台币余额 / 状态”字段；历史数据库自动补列，历史已支付平台币订单会在首次升级时回填平台币余额。
- 新建已支付平台币订单会同步增加玩家平台币余额；超管手工发放/收回会写入平台币变动审计表。

## V31 流水统一口径

V31 将系统内所有“流水/充值/分佣”统一为同一数据源：仅统计 `pay_status=paid` 且存在 `paid_at` 的平台币充值订单。

- 数据总览：总流水 / 昨日流水 / 今日流水仅统计真实已支付平台币订单。
- 代理列表：今日 / 昨日 / 总流水使用同一口径实时计算。
- 渠道结算与分佣：仅按真实已支付平台币订单金额计算。
- 玩家今日充值 / 总充值：仅统计真实已支付平台币订单。
- 商城订单不计入流水、充值累计或代理分佣。
- 超管手工发放/收回平台币只影响玩家余额并写入 `player_coin_ledger`，不计入任何流水或分佣。
- 升级启动时会纠正旧版本中被商城订单污染的代理流水和玩家充值缓存，但不会重算 `platform_coin_balance`，因此手工发放/收回后的实际余额不会被覆盖。

## V33
- 超管修改代理/玩家归属支持搜索代理ID、账号、名称。
- 归属默认保持不变，只有主动选择新归属才会修改。
- 后端同步增加空值防误操作保护。


## V35 北京时间统一与代理登录时间

- 数据库继续统一保存 UTC，后台所有日期时间通过 `Asia/Shanghai` 转换为北京时间展示，避免 Render/服务器地区造成 8 小时时差。
- 玩家列表的注册时间、最后登录时间统一显示北京时间。
- 代理列表新增“创建时间(北京时间)”与“最近登录(北京时间)”。
- 代理每次成功登录后台都会更新最近登录时间。
- `render.yaml` 与生产配置显式设置 `BUSINESS_TIMEZONE=Asia/Shanghai`。
- 订单创建/支付、发货、CDK、邮件、领取等所有通过公共 `dt()` 输出的时间也会同步按北京时间展示。


## V36 代理列表布局
- 创建时间、最近登录时间移动到佣金比例右侧。
- 复制注册地址移入最右侧操作列，与编辑按钮并排显示。


## V37 代理注册人数

- 代理列表新增“注册人数”列。
- 注册人数统计口径：仅统计通过该代理专属注册链接直接注册并归属到该代理的玩家，不包含其下级代理的玩家。
- “注册人数”显示在“上级代理”右侧、“今日流水”左侧。


## V38 渠道结算调整

- 代理列表不再展示今日流水、昨日流水、总流水；注册人数、佣金比例、创建时间、最近登录时间和操作区保持不变。
- 渠道结算改为按北京时间自然日查看代理真实支付平台币流水，默认日期为当天。
- 渠道结算查询顺序：账号查询、ID 查询、等级查询（一级/二级/三级）、日期选择。
- 所选日期的代理流水默认按金额从高到低排序；流水为 0 的代理仍会展示在后面。
- 超级管理员可查看全部代理；普通一级/二级代理只查看自己的下级代理树，不包含自己；三级代理仍无渠道管理权限。
- 流水继续严格限定为 `pay_status=paid` 且 `paid_at` 有值的平台币订单。手工发放/收回平台币、商城订单、未支付订单均不进入流水与佣金。
- 原 `/api/settlements` 历史结算单接口保留兼容；后台渠道结算页面使用新的 `/api/channel-settlements` 日流水接口。

## V39 渠道结算总流水 / 日期区间

- 渠道管理代理列表与渠道结算列表均去掉“上级代理”展示列，列表更紧凑。
- 渠道相关表格单元格横纵留白约缩小 30%，查询栏间距同步收紧。
- 渠道结算默认不选择日期；不选日期时展示每个代理的全部历史真实支付总流水，并按流水从高到低排序。
- 日期查询升级为紧凑的“开始日期 至 结束日期”双日期选择器：只选一个日期时按该单日统计，同时选择两端时按包含首尾日期的区间累计统计。
- 查询顺序仍为账号、ID、等级、日期；日期输入默认空白，不再自动填当天。
- 渠道结算表将“日期 / 当日流水 / 当日佣金”统一为“统计范围 / 流水 / 佣金”，总流水显示“全部时间”，单日显示日期，区间显示“开始日期 至 结束日期”。
- 超管仍查看全部代理；普通一级/二级代理只查看自己的下级代理树，不包含自己。
- 流水口径不变：只统计 `pay_status=paid` 且 `paid_at` 有值的平台币订单；手工平台币调整、商城订单、未支付订单均不计入。

## V42 渠道结算日期区间与横向紧凑修复

- 渠道结算日期恢复为“开始日期 至 结束日期”双日期选择器，默认不选择日期。
- 不选日期显示历史总流水；同一天显示当日流水；不同开始/结束日期显示区间流水，区间包含首尾两个北京时间自然日。
- 表格只压缩左右间距约 30%，上下行高保持不变；查询栏同样仅收紧横向空间。
- 保留 V41 的分层等级筛选权限和三级代理无渠道结算权限规则。

## V45 UI 调整
渠道结算列表的表头与数据行纵向高度已与“下级渠道”列表统一；左右紧凑间距保持不变。


## V47 支付测试页
详见 `README_V47.md`。仅超管可使用，用于无需 Postman 完整模拟平台币下单与支付成功。

## V48 平台币订单状态
平台币订单状态只区分“未支付 / 已支付”，发货成功或失败在独立“发货”列展示。已支付筛选包含所有真实已支付订单，不再提供“已发货”状态筛选。


## V49 平台币发货确认
支付成功和发货成功分离；只有平台币实际入账事务提交成功后，订单发货状态才显示成功。失败订单保留补发入口，且补发幂等。


## V53 商城购买计入累计充值

详见 `README_V53.md`。商城礼包购买增加玩家累计充值奖励进度，但不增加渠道流水和代理佣金。


## V55 手工平台币补偿口径

- 超管手工补偿/发放、收回平台币只修改玩家余额并写 PlayerCoinLedger 审计。
- 手工平台币调整绝不创建/修改真实支付订单，因此不进入真实流水，不产生代理分佣，也不增加累计充值。
- 玩家真实平台币充值只计算真实流水与分佣；玩家随后在网页商城实际消费平台币才增加累计充值奖励进度。
- 典型场景：商城礼包异常未到账但平台币已扣，可由超管手工补偿等额平台币，不会造成二次流水或二次分佣。


## V57
商城订单新增账号/商品查询，并记录购买时具体角色名和区服快照。玩家中心多角色账号购买礼包时需要选择角色；历史订单不猜测角色。详见 `README_V57.md`。

## V58 玩家中心商城交互
玩家中心礼包商城改为下拉选择礼包；选中后自动显示礼包名称、SKU、平台币售价、库存与礼包内容。页面不再显示“使用平台币直接购买；购买成功后自动生成商城订单”等说明文字。


## V59
玩家中心入口顺序：平台币充值 → 购买礼包 → 领取累充 → 特权卡。
