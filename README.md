# V43 渠道结算表格稳定布局修复

- 修复渠道结算在“日期查询”和“重置日期”之间切换时，浏览器根据内容重新计算列宽导致列表左右跳动。
- 渠道结算 7 列改为固定比例布局，查询总流水、单日流水、日期区间流水时列位置保持一致。
- 保持 V42 的行高与横向间距：上下 11px、左右 7px，不再改变用户要求的间距。
- 为页面滚动条预留稳定空间，减少查询结果行数变化造成的整体页面横向位移。
- 不修改 V42 的日期区间、等级权限、真实支付流水统计和三级代理权限。
