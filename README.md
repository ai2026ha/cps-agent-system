# V34 玩家多区服角色补丁

适用于 V33 升级。覆盖补丁中的 `backend/` 对应文件后提交 GitHub，等待 Render 自动部署。

主要变化：

- 新增 `player_characters` 数据表：一个玩家账号可绑定多个区服角色。
- 玩家列表合并“角色名 / 区服”为一个“角色”下拉列。
- 默认只显示主角色名，展开后查看全部角色及对应区服。
- 历史有效角色/区服会自动迁移，不需要删除 Render PostgreSQL。
