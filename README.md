# V38 渠道结算补丁

从 V37 升级时，将补丁内文件按原目录覆盖到 GitHub 仓库即可，不需要删除 PostgreSQL 数据库。

本补丁包含：
- 代理列表移除今日/昨日/总流水三列。
- 渠道结算按所选北京时间日期展示真实支付平台币流水，并按流水从高到低排序。
- 查询栏：账号、代理 ID、代理等级、日期；日期框保持紧凑并使用浏览器原生日历选择器。
- 超管看全部代理；普通代理只看自己的下级代理树，不包含自己。
- 新增 `/api/channel-settlements`，旧结算单接口继续保留。
