CPS V11 补丁

改动：
1. 超级管理员创建的一级代理，上级代理在界面统一显示“超管”。
2. 二级、三级代理仍显示真实上级代理ID。
3. 上级代理查询支持输入“超管”或“超级管理员”筛选一级代理。
4. 数据库内部一级代理 parent_id 仍为空，不伪造普通代理父节点。
5. 静态资源版本号升级，避免 Render 部署后浏览器继续使用旧缓存。

覆盖到 GitHub 相同路径后提交，等待 Render 自动部署，完成后 Ctrl+F5 强制刷新。
